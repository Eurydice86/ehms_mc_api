# MyClub API Configuration
MC_TOKEN="your-myclub-api-token-here"

# BigQuery Configuration
GCP_PROJECT_ID="your-gcp-project-id"
BIGQUERY_DATASET_ID="ehms_myclub"

# Optional: Path to GCP service account JSON file
# If not set, will use default application credentials
# Uncomment and set the path to use service account authentication
# GOOGLE_CREDENTIALS_PATH="/path/to/service-account-key.json"
